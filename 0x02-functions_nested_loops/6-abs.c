#include <stdlib.h>
#include <time.h>
#include <holberton.h>
/**
* main - print alphabet
*
* Return:0  (sucess)
*/
int _abs(int n )
{
if (n > 0)
{
return(n);
}
if elsr(n < 0)
{
return(n = n * -1);
}
else
return (0);
}
